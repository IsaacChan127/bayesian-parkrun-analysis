model2 <- brm(
  Time ~ Gender + Location,
  data = combined_data,
  family = gaussian(),
  prior = c(
    prior(normal(0, 5), class = "b"),
    prior(normal(20, 5), class = "Intercept"),
    prior(exponential(1), class = "sigma")
  ),
  chains = 4,
  cores = 4,
  seed = 42
)

# Compare models
loo(model2, model2_hier)
